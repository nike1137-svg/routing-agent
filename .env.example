# .env.example 를 .env 로 복사한 뒤 키를 입력한다. .env 는 저장소에 올라가지 않는다(.gitignore).
OPENAI_API_KEY=

# 모델 (단계별로 분리)
MODEL_ROUTER=gpt-5.6-luna
MODEL_ANSWER=gpt-5.6-luna
MODEL_JUDGE=gpt-5.6-luna

# 누적 API 비용이 이 금액(USD)을 넘으면 호출을 멈춘다
COST_LIMIT_USD=1.5
